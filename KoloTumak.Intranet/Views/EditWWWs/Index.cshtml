@model IEnumerable<KoloTumak.Data.Data.CMS.EditWWW>

@{
    ViewData["Title"] = "Index";
}

<div class="home">
<header class="header__intranet flex-container">
<div class="flex-items">
<h1 class="text__intranet__header">Edycja WWW</h1>
</div>
<div class="flex-items">
<p class="button__header">
    <a class="btn__header" asp-action="Create">Dodaj nav linki</a>
</p>
</div>
</header>
<table class="table">
    <thead class="table__header">
        <tr class="table__row">
            <th class="table__cell u-text-center">
                @Html.DisplayNameFor(model => model.NameNavSite)
            </th>
            <th class="table__cell u-text-center">
                @Html.DisplayNameFor(model => model.NameSite)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td class="table__account table__cell u-text-center">
                @Html.DisplayFor(modelItem => item.NameNavSite)
            </td>
            <td class="table__account table__cell u-text-center">
                @Html.DisplayFor(modelItem => item.NameSite)
            </td>
            <td class="table__account table__cell u-text-center">
                <a class="btn" asp-action="Edit" asp-route-id="@item.IdPosition">Edytuj</a>
                <br></br>
                <a class="btn" asp-action="Delete" asp-route-id="@item.IdPosition">Skasuj</a>
            </td>
        </tr>
}
    </tbody>
</table>
</div>

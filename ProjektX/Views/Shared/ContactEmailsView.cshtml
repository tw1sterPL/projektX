@model IEnumerable<KoloTumak.Data.Data.CMS.ContactEmail>

@foreach(var item in Model){
    <h4 class="text-uppercase m-0">@item.NameTextEmail</h4>
    <hr class="my-4 mx-auto" />
    <div class="small text-black-50"><a href="#!">@item.Email</a></div>
}
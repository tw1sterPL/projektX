@model IEnumerable<KoloTumak.Data.Data.CMS.ContactAdres>

@foreach(var item in Model){
    <h4 class="text-uppercase m-0">@item.NameTextAdres</h4>
    <hr class="my-4 mx-auto" />
    <div class="small text-black-50">@item.Adres</div>
}